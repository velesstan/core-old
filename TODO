C:\Users\Evgheni\Desktop\velesstan-core\node_modules\.cache\mongodb-memory-server\mongodb-binaries\4.0.14\mongod.exe


MODIFY ENV constants (auth)
ADD ROLE-based auth
DOCKER COMPOSE & ENV -> drive
REDUCE -: percents or value
PRODUCT Transaction snapshot (price)
set server time & sync ( cp /usr/share/zoneinfo/Europe/Chisinau /etc/localtime )


QUERY DTO


MONGODUMP 
    docker-compose exec -T database sh -c 'mongodump --archive -d DBNAME -u USER -p PASSWORD --authenticationDatabase admin' > db.dump
    docker-compose exec -T database sh -c 'mongorestore --archive -u USER -p PASSWORD' < db.dump

Script -> import all residues
EXPORTING PRODUCTS to excel (pagination support)n+ sort code

Накладные сворачивать